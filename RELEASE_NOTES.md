<!-- Release notes for the NEXT tag. Updated in the release commit; CI bakes
     this file into the GitHub Release body AND latest.json's notes, which the
     in-app update modal shows. Keep BOTH sections — the app picks the user's
     language ("## What's new" for EN, "## 更新内容" for ZH) and strips the
     bullets. History lives in git; this file only ever describes the upcoming
     release. -->

## What's new

- The app now offers updates on launch: when a new version ships, a prompt
  shows what changed and installs it in one click.

## 更新内容

- 启动时更新提醒：新版本发布后弹出更新内容，一键安装并重启。
